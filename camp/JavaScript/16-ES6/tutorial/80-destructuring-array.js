/**
 * Array Destructuring helps us create variables with values
 * from the Array we specify at the right of the `=` sign
 */
const [firstName, lastName] = ['Peter', 'Pan'];

console.log(firstName); // Peter
console.log(lastName); // Pan
